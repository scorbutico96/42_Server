@@ -18,15 +18,15 @@
 
 # Default server configuration
 #
+#server {
+#	listen 80 default_server;
+#	listen [::]:80 default_server;
+#	return 301 https://$host$request_uri;
+#	}
+
 server {
 	listen 80 default_server;
 	listen [::]:80 default_server;
-	return 301 https://$host$request_uri;
-	}
-
-server {
-	#listen 80 default_server;
-	#listen [::]:80 default_server;
 
 	# SSL configuration
 	#
